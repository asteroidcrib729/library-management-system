# This is the only backend location exposed to browser code.
# It is public at build time and must never contain credentials.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_CSRF_COOKIE_NAME=lms_csrf
